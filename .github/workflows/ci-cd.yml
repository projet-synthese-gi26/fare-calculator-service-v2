name: Spring Boot CI/CD

on:
  push:
    branches:
      - "**"
jobs:
  pipeline:
    uses: projet-synthese-gi26/workflows/.github/workflows/django-ci-cd.yml@main
    with:
      app_name: fare-calculator-service
    secrets: inherit          # passe TOUS les secrets automatiquement